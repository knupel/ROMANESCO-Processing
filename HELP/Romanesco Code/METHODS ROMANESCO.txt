// GLOBAL methods or variables for CONTROLLER, SCENE and PRESCENE
//////////////////////////////////////////////////////////////////


// STRING METHOD
////////////////





// FLOAT METHOD
float truncate( float x ) @return float

String joinIntToString(int []data) @return the int list in a String separate by "/" must be improve, to add a choice in separator

String joinFloatToString(float []data) @return the float list in a String separate by "/" must be improve, to add a choice in separator

String FloatToString(float data) @translate a float to String with "1" float or with "3", weird must look inside

String IntToString(int data) @translate an Integer in String

String [] splitText(String textToSplit, String separator) @return a list of String

int longestWord( String[] listWordsToSort) @return the num of Char of String

int longestWord( String[] listWordsToSort, int start, int finish ) @return the num of Char of String between two points

int longest_word_in_pixel( String[] listWordsToSort) @return length in Pix of biggest String in a String list for an font size of 10

int longest_word_in_pixel( String[] listWordsToSort, int start, int finish ) @return length in Pix of biggest String in a String list for an font size of 10, between two points

int length_String_in_pixel(String target, int ratio) @return a length in Pix of one String

boolean research_in_String(String research, String target) @return a boolean if String is find in an other String


// PVECTOR
///////////
PVector gotoTarget(PVector origin,  PVector finish, float speed) // nice for a static travel when the start and the arrival is knew, work with XYZ

PVector follow(PVector target, float speed) // go to follow something in move like a cursor, works with XYZ


PVector [] circle (PVector pos, int d, int num) // return a list of points xyz to realize a circle, the first param is the position of circle, the second is the diameter, the third is the num of points to do this circle
PVector [] circle (PVector pos, int d, int num, float jitter) // same function with jitter variation

PVector pointOnCirlcle(int r, float angle) // return the position of the point on the perimeter of the circle. The first param is the radius, the second is the angle in radian (PI scale)

float perimeterCircle (int r) return perimeter

float radiusSurface(int surface) return the radius with a speficic surface

float angle(PVector p0, PVector p1) calcul the angle between two PVectors

PVector normalDir(int direction) // return a direction between -1 / 1 from 360° direction






// TIME
///////
float cycle(float add) // return value between -1 et 1 , nice to try with value like 0.1

int timer(float tempo) // return an add value from float <1 ;

int minClock() // return the time of the day in minutes










// LOCAL methods or variables for SCENE and PRESCENE
//////////////////////////////////////////////////////////////////
// WHERE are Romanesco work
if you want use a different rendering on the scene or prescene, you can use those booleans.

boolean fullRendering 
> to choice tha quality of rendering, for the Prescene is false, for the Scene is true

boolean internet 
> @ return boolean if internet is available

boolean scene 
boolean prescene
> this way is deprecated









































